export class SimpleCartItem {

  id: number;
  title: string;
  amount: number;

  constructor(id: number, title: string, amount: number) {
      this.id = id;
      this.title = title;
      this.amount = amount;
  }

}
